class Supplier {
  id!: number;
  phone!: string;
  name!: string;
  address!: string;
  description!: string;

  // public cssClassDoned = (): string => {
  //   return this.done ? 'completed' : '';
  // };
}

export default Supplier;
