class Customer {
  id!: number;
  phone_number!: string;
  name!: string;
  address!: string;

  // public cssClassDoned = (): string => {
  //   return this.done ? 'completed' : '';
  // };
}

export default Customer;
